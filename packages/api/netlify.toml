[build]
  command = "npm install -g pnpm && cd ../.. && pnpm install && pnpm --filter @legal-chatbot/shared build && pnpm --filter @legal-chatbot/api build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
