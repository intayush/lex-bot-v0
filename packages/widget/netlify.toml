[build]
  command = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @legal-chatbot/shared build && pnpm --filter @legal-chatbot/widget build && cp -r chatbot-context packages/widget/dist/chatbot-context"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# CORS headers for chatbot-context files
[[headers]]
  for = "/chatbot-context/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "public, max-age=300"
